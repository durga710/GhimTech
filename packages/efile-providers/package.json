{
  "name": "@ghimtech/efile-providers",
  "version": "0.1.0",
  "private": true,
  "description": "E-file provider adapters for GhimTech Tax: mock, sandbox, and placeholder adapters for authorized transmitters",
  "author": "Durga Ghimeray",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@ghimtech/efile-core": "workspace:*",
    "@ghimtech/tax-domain": "workspace:*"
  },
  "devDependencies": {
    "@ghimtech/config": "workspace:*",
    "@ghimtech/testing": "workspace:*",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
