packages:
  - 'apps/*'
  - 'packages/*'
allowBuilds:
  # esbuild is the bundler vitest and Next.js run on; sharp is Next.js image
  # optimisation. Both need their postinstall to place a platform binary.
  esbuild: true
  sharp: true
