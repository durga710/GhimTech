{
  "name": "@ghimtech/worker",
  "version": "0.1.0",
  "private": true,
  "description": "GhimTech Tax background worker: acknowledgment polling, OCR processing, communications",
  "author": "Durga Ghimeray",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "echo worker logic covered by api and package tests",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "bullmq": "^5.34.8",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@ghimtech/config": "workspace:*",
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
